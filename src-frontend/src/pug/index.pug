html(lang="en")
    head
        title LateUnicorn.com - Every project has a unique story, and yours deserves to be told too
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
        link(rel="preconnect", href="https://fonts.gstatic.com")
        link(href="https://fonts.cdnfonts.com/css/satoshi?styles=135009,135005,135007,135002,135000", rel="stylesheet")
        link(rel="stylesheet", href="/css/tailwind/tailwind.min.css")
        link(rel="icon", type="image/png", sizes="32x32", href="/favicon.png")
        script(defer, src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2")
        script(defer, src="/js/main.js")
        script(defer, src="/js/index.js")
    body(class="antialiased bg-body text-body font-body bg-gray-900")
        div(class="")
            include partials/navbar.pug

            section(class="pb-8 overflow-hidden")
                div(class="container mx-auto px-4")
                    div(class="flex flex-wrap justify-center -m-4 mt-5 md:-mt-4")
                        div(class="w-full md:w-auto p-4 hidden lg:block")
                            img(class="mx-auto rounded-3xl", src="/images/late-unicorn-hero.jpg", alt="")
                        div(class="w-full md:flex-1 p-4")
                            div(class="relative flex flex-col justify-center items-center p-8 h-full text-center bg-gray-800 overflow-hidden rounded-3xl")
                                div(class="absolute bottom-0 left-1/2 min-w-max transform -translate-x-1/2")
                                    div(class="absolute bg-gradient-radial-dark-light w-full h-full")
                                    img(src="/images/pattern-grid-dark-light.svg", alt="")
                                div(class="relative max-w-lg")
                                    h1(class="font-heading mb-6 text-4xl md:text-5xl text-white font-black tracking-tight")
                                        span Explore the untold stories of projects and ideas that&nbsp;
                                        span(class="text-blue-500") almost changed the world
                                    p(class="mb-8 text-xl text-gray-500 font-bold") Preserve the memories of your projects that didn't make it. They might be dead, but their history is worth sharing, and their legacy may inspire new initiatives!
                                    div(class="flex flex-wrap justify-center -m-2")
                                        div(class="w-full md:w-auto p-2")
                                            a(class="block w-full px-6 py-3 text-md text-center text-white font-bold bg-blue-500 hover:bg-blue-600 focus:ring-4 focus:ring-blue-200 rounded-full", href="/login/") Get Started
            section(class="pt-10 pb-8 overflow-hidden")
                div(class="container mx-auto px-4")
                    div(class="py-16 px-8 bg-gray-800 rounded-3xl")
                        div(class="max-w-7xl mx-auto")
                            div(class="mb-10")
                                h2(class="font-heading mb-6 text-4xl md:text-5xl lg:text-6xl text-white font-black tracking-tight") Explore The Ceased Projects
                                p(class="text-gray-400 font-bold text-xl") Browse through the collection of projects that didn't make it, keep their legacy alive, and discover valuable insights for your next creation.
                            div(id="project-list", class="flex flex-wrap -m-4 mb-12")
                                div(class="w-full mt-20 text-center")
                                    img(class="w-20 h-20 inline-block animate-spin", src="/images/spinner.svg", alt="")

                                div(id="project_card_template", class="hidden")
                                    //- This is the template that is used to generate project cards in the JS. The JS code will
                                    //- clone this template and fill it with the project data and then append it to the project list
                                    div(class="w-full md:w-1/2 p-4")
                                        div(class="p-8 h-full bg-gray-900 rounded-3xl")
                                            div(class="flex flex-wrap -m-3")
                                                div(class="w-auto p-3")
                                                    a(href="{url}")
                                                        img(class="w-44", alt="{name}", title="{name}", {cover_attr_src}="{cover_url}")
                                                div(class="flex-1 mt-2 p-3")
                                                    div(class="flex flex-col justify-between h-full")
                                                        div(class="flex-initial")
                                                            p(class="mb-3 text-xs text-gray-500 font-bold") {categories}
                                                            a(class="group inline-block" href="{url}")
                                                                h3(class="font-heading text-xl text-white hover:text-gray-200 group-hover:underline font-black") {name}
                                                            a(class="group inline-block" href="{url}")
                                                                p(class="text-sm text-gray-400 hover:text-gray-300 group-hover:underline") {description}

            include partials/footer.pug
